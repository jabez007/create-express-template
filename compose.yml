services:
  app:
    #depends_on:
    #  db:
    #    condition: service_healthy
    build:
      context: .
    ports:
      - "8080:8080"
    environment:
      NODE_ENV: local
      PORT: 8080
    #env_file:
    #  - ".env"
    #  - ".env.local"
    #restart: on-failure
    develop:
      watch:
        - path: ./src
          action: rebuild
